[build]
framework = "static-site"
# Removed buildCommand because 'node' is not available in Pxxl static environment.
# Relying on Pxxl to serve the files directly from the repository root.
outputDirectory = "."
